{
  "name": "video-quality-enhancer",
  "version": "1.0.0",
  "description": "AI Video Upscaler with React and FastAPI",
  "scripts": {
    "frontend": "npm run dev --prefix frontend",
    "backend": "cd backend && venv\\Scripts\\activate && python app.py",
    "setup": "npm install && npm run setup:frontend && npm run setup:backend",
    "setup:frontend": "npm install --prefix frontend",
    "setup:backend": "cd backend && python -m venv venv && venv\\Scripts\\activate && pip install -r requirements.txt",
    "dev": "npx concurrently \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}


